version: "3.8"

services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
    restart: unless-stopped
    secrets:
      - cloudflared_token
    environment:
      TUNNEL_TOKEN: /run/secrets/cloudflared_token
secrets:
  cloudflared_token:
    external: true